def capitalize(s1):
    s1=res=s1.title()
    res=""
    for w in s1.split():
        res+=w[:-1]+w[-1].upper() + " "
    return res[:-1]
output=input("enter your string")    
print(capitalize(output))
